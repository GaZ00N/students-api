apiVersion: v1
kind: Service
metadata:
  name: students-api-service
  namespace: students-api
spec:
  type: NodePort
  selector:
    app: students-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
